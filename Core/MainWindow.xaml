<Window x:Class="MrKeys.MainWindow"
        xmlns:local="clr-namespace:MrKeys"
        xmlns:cm="clr-namespace:Common.Media;assembly=Common"
        xmlns:kb="clr-namespace:KeyBoardControlLibrary;assembly=KeyBoardControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="MainWindow" Height="300" Width="300">
    <StackPanel d:DataContext="{d:DesignInstance local:MainWindowViewModel}" DataContext="{Binding}">
        <!--<cm:MediaControl/>-->
        <Viewbox Margin="5">
            <kb:KeyBoardControl/>
        </Viewbox>
        <TextBlock Text="{Binding Path=TextBoxTest}" Height="30" Width="100" />
    </StackPanel>
</Window>
